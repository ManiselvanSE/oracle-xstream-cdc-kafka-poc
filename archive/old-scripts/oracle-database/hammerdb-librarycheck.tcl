# Non-interactive Oracle (and other) client library check for HammerDB CLI.
# Usage:  source hammerdb-oracle-env.sh && hammerdbcli tcl auto hammerdb-librarycheck.tcl
# Do NOT pipe to hammerdbcli without a TTY — use "tcl auto" instead.

librarycheck
